// Test for-loop compilation to WASM
int sum_range(int n) {
    int sum = 0
    for (i, n) {
        sum = sum + i
    }
    return sum
}

void main() {
    int result = sum_range(100)
    println(str(result))
}
